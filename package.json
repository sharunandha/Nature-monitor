{
  "name": "environment-flood-landslide",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm run build",
    "postinstall": "cd backend && npm install && npm run build:frontend",
    "start": "cd backend && npm start"
  }
}
